# MicroChat
A simple chat interface that lets users send, view, edit, and delete messages. Each message includes sender/receiver details along with date and time. All chat data is stored and managed using MongoDB. The UI is minimal and user-friendly, showcasing basic CRUD operations in a messaging environment.
